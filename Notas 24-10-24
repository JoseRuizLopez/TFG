min(15% total de imagenes, 80% de los 1 que tengo)  80% o ver que porcentaje cambiar. Tiene sentido cambiar mucahs porque hay muchas opciones dispobinles

calcula(length, ratio)=min(length*0.15, length*ratio*0.8)

boxplot:
    * FIjando Algritmo, midiendo porcentajes
    * Fijando porcentaje, midiendo algoritmo

Dataset paintings solo con MobileNet:
Probar con 10% y 100%

genetico_v3: si el segundo mejor de la poblacion no mejora, reinio algoritmo (me quedo con la mejor, pero genero de nuevo las otras 9)